"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const main_1 = tslib_1.__importDefault(require("./main"));
const db = new main_1.default.Database({
    tables: ['main', 'Users', 'Arrays', 'Tests']
});
db.on('ready', db => {
    console.log('Database opened.');
});
(async () => {
    let before = await db.get('idk.array', 'Tests');
    console.log('before: ' + before);
    await db.remove('idk.array', 'Adioh', 'Tests');
    await db.add('idk.number', 3, 'Tests');
    await db.sub('idk.number', 2, 'Tests');
})();
db.start();
