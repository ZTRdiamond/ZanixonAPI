"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.data = void 0;
const builder_1 = require("../classes/builder");
const utils_1 = require("../classes/utils");
exports.data = {
    data: new builder_1.FunctionBuilder()
        .setName('findSplitIndex')
        .setValue('description', 'Finds a text into a splitted text, if the text is found, returns its index, otherwise returns 0.')
        .setValue('use', '$findSplitIndex[query]')
        .setValue('returns', 'Number'),
    code: async (d) => {
        let r = d.unpack(d);
        if (!r.inside)
            return utils_1.Utils.Warn('Invalid inside provided in:', d.func);
        let [query] = r.splits;
        let arr = d._.splits;
        if (!arr)
            return utils_1.Utils.Warn('There is not saved splits yet, use $split first. Error in:', d.func);
        return {
            code: d.code.resolve(`${d.func}[${r.inside}]`, arr.indexOf(query.unescape()) + 1 || '0')
        };
    }
};
