"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.data = void 0;
const builder_1 = require("../classes/builder");
const utils_1 = require("../classes/utils");
exports.data = {
    data: new builder_1.FunctionBuilder()
        .setName('getVar')
        .setValue('description', 'Get the variable value in the database.')
        .setValue('use', '$getVar[name]')
        .setValue('returns', 'Any'),
    code: async (d) => {
        let r = d.unpack(d);
        if (!d.interpreter.db)
            return utils_1.Utils.Warn('No database set yet, error in:', d.func);
        if (!r.inside)
            return utils_1.Utils.Warn('Invalid inside provided in:', d.func);
        let v = await d.interpreter.db.get(r.inside.unescape());
        return {
            code: d.code.resolve(`${d.func}[${r.inside}]`, typeof v !== 'object' ? v.escape() : JSON.stringify(v, null, 2).escape() || 'undefined')
        };
    }
};
