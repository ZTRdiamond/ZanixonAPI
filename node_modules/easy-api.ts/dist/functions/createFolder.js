"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.data = void 0;
const tslib_1 = require("tslib");
const builder_1 = require("../classes/builder");
const utils_1 = require("../classes/utils");
const fs_1 = tslib_1.__importDefault(require("fs"));
exports.data = {
    data: new builder_1.FunctionBuilder()
        .setName('createFolder')
        .setValue('description', 'Creates a directory using path as reference.')
        .setValue('use', '$createFolder[path]')
        .setValue('returns', 'Void'),
    code: async (d) => {
        let r = d.unpack(d);
        if (!r.inside)
            return utils_1.Utils.Warn('Invalid inside provided in:', d.func);
        let [path] = r.splits;
        if (!path)
            return utils_1.Utils.Warn('Path is required in:', d.func);
        try {
            fs_1.default.mkdirSync(path);
            return {
                code: d.code.resolve(`${d.func}[${r.inside}]`, '')
            };
        }
        catch (err) {
            return utils_1.Utils.Warn('Error trying to create the folder.', d.func);
        }
    }
};
