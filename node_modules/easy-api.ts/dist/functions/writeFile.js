"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.data = void 0;
const tslib_1 = require("tslib");
const builder_1 = require("../classes/builder");
const utils_1 = require("../classes/utils");
const fs_1 = tslib_1.__importDefault(require("fs"));
exports.data = {
    data: new builder_1.FunctionBuilder()
        .setName('writeFile')
        .setValue('description', 'Replaces any content the provided content in the provided file, if the file exists.')
        .setValue('use', '$writeFile[path;content]')
        .setValue('returns', 'Void'),
    code: async (d) => {
        let r = d.unpack(d);
        if (!r.inside)
            return utils_1.Utils.Warn('Invalid inside provided in:', d.func);
        let [path, content] = r.splits;
        if (!path)
            return utils_1.Utils.Warn('Path is required in:', d.func);
        if (!content)
            return utils_1.Utils.Warn('Content is required in:', d.func);
        try {
            fs_1.default.writeFileSync(path, content);
            return {
                code: d.code.resolve(`${d.func}[${r.inside}]`, '')
            };
        }
        catch (err) {
            return utils_1.Utils.Warn('Error trying to write the given file.', d.func);
        }
    }
};
